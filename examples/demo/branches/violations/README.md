# orders-service

Serves order records over HTTP. Components read through the typed client in
`src/api/client.ts`; schema changes live in `migrations/`.

## Refunds

Our blazing-fast refund pipeline delivers a seamless, world-class experience at
enterprise scale, with best-in-class reliability your customers will love.

## Running the checks

```bash
python tests/check_rules.py
```
