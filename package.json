{
  "name": "ai-doc-sync-hook",
  "version": "0.1.0",
  "description": "Modular AI docs sync pre-push workflow runner",
  "license": "MIT",
  "bin": {
    "ai-doc-sync-hook": "bin/ai-doc-sync-hook.js"
  },
  "scripts": {
    "test": "uv run --with pytest pytest tests -q"
  },
  "files": [
    "bin",
    "src",
    "README.md",
    "run.sh",
    "pyproject.toml",
    ".ai-doc-sync.toml"
  ],
  "engines": {
    "node": ">=18"
  }
}
