# ai-doc-sync-hook

Standalone extracted history for the AI docs sync hook runtime.

## Layout

- `hook.py` - hook runtime
- `run.sh` - shell entry wrapper
- `prompts/` - stage prompt assets
- `.ai-doc-sync.toml` - default config snapshot from source workspace
